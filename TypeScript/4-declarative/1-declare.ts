/// <reference path='globals.d.ts' />
console.log(message);
hello();
let result = sum (2, 4);
console.log(result);

tom.print();

for (const point of points) {
    console.log(`Coords of point: x:${point.x} y:${point.y}`)
}

const bob = new Person('Bob', 37);
bob.display();
console.log(bob.name);